apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
data:
  policy.overlay.csv: |
    p, role:user, projects, get, workloads, allow
    p, role:user, applications, workloads/*, *, allow
    g, christian, role:user
